/**
 * Inicializaciones generales del sitio.
 * Usa este archivo para comportamientos globales que se compartan entre paginas.
 */
document.addEventListener('DOMContentLoaded', function () {
    document.documentElement.classList.add('site-ready');
});
