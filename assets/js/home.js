/**
 * Logica especifica del inicio.
 * Usa este archivo para sliders, bloques y comportamiento exclusivo del home.
 */
document.addEventListener('DOMContentLoaded', function () {
    const homeMarker = document.querySelector('.banner-six-area');

    if (!homeMarker) {
        return;
    }

    console.log('Home de COLEGIO_SPABLO listo para personalizaciones.');
});
